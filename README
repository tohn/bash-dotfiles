Bash-dotfiles
by Yannic Haupenthal

This repos contains my bash dotfiles, i.e. my .bashrc.

To link one or all files to your home, execute:
ln -s /path/to/repos/.bashrc ~/.bashrc
or:
ln -s /path/to/repos/.bash* ~
